version: '3.4'

services:
  client:
    container_name: Guardian-Frontend
    image: client
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 5173:5173
    volumes:
      - .:/usr/src/app